# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (server/.env, .env, etc.) — keep .env.example tracked
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.scratch/

# Raw/unused source photography kept locally as a reference pool — not
# imported anywhere in src, and far too large (hundreds of MB) to track in
# git or ship on every clone/deploy pull.
src/assets/images/backup-pool/
